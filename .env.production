# Google Analytics. Public by design: this value ships inside the HTML of every
# page, which is how a browser knows where to report. Committed rather than kept as
# a build variable so every host gets it without anybody remembering to set it.
VITE_GA4_ID=G-KDT0D0YLCE
