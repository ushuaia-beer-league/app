/**
 * Placeholder shell. Phase 3 of `docs/plan.md` replaces it with the ported
 * reference design; it exists now so the pipeline has an application to build.
 */
export function App() {
  return (
    <main>
      <h1>Ushuaia Beer League</h1>
      <p>Sitio en construcción.</p>
    </main>
  )
}
